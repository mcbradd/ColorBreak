# ColorBreak — Spec (single source of truth)

Consolidates the original product plan (the `grill-with-docs` session that produced
[CONTEXT.md](CONTEXT.md), [DESIGN.md](DESIGN.md), and `docs/adr/`) with the revisions in
[docs/colorbreak-feedback-2026-08-10.md](docs/colorbreak-feedback-2026-08-10.md) (owner
playtest, 2026-08-10). This file is the anchor for what the product must do; it does not
restate law that already lives elsewhere:

- **Vocabulary** — [CONTEXT.md](CONTEXT.md)
- **Visual system, verdicts, Break Bar** — [DESIGN.md](DESIGN.md) ("The Board" — locked, do not re-litigate)
- **Point decisions with rationale** — `docs/adr/`
- **Collation data pipeline (v2)** — `tools/README.md`

## Product statement

A price board for MTG Whatnot color breaks. Mobile-first (one-handed, portrait), with
mouse/keyboard as a working secondary target. Primary question, answered fast: *does this
slot have an edge at this price?* Buyers and sellers get separate views of the same
composition — a break of any mix of boxes/packs/bundles across sets — priced from live
Scryfall + tcgcsv market data.

## Status: the 2026-08-10 feedback items are shipped

The feedback doc's 14 items (P0 product/cost model, P1 drill-down, P2 polish) are the
active requirements baseline. All are implemented in the current build; the doc's
acceptance criteria remain the regression checklist. Verified against code, not against
memory of past sessions:

| # | Requirement | Where it lives |
|---|---|---|
| 1 | Mobile-first redesign, no clipping at 390px | `index.html` layout; gate `assertG1`/`assertG2` in `test/e2e/harness.mjs` |
| 2 | Set picker (search/sort/filter), no memorized codes | set-picker bottom sheet, `index.html` |
| 3 | Split fee model: seller fulfillment cost vs. buyer S&H | `orderNet`, `compBreakEven` (`index.html`), pinned by `test/fees.test.mjs` |
| 4 | Buyer view / seller view | `body[data-view]` toggle, `.buyer-only`/`.seller-only` (`index.html:52-53,210`) |
| 5 | Break composition builder (multi-product, multi-set) | `comp` line-item array `{set,key,qty,ub,basis,paid}`, `?b=` URL codec |
| 6 | Per-set static product catalog | `data/products.json`, generated by `tools/build-products.mjs` |
| 7 | Cost basis: sealed-box fraction, loose-pack fallback | `chooseBasis` (`index.html`), pinned by tests |
| 8 | Slot drill-down: top 10 contributors | slot-drill bottom sheet |
| 9 | Remove $1 floor; optional threshold, off by default | `computeEV(..., minValue)` with `excluded` tracking |
| 10 | Auto-computed break-even, auto-populated seller costs | `compBreakEven` + pricing desk |
| 11 | Card popups: image + Oracle text | card-popup bottom sheet |
| 12 | Desk recast around buyer/seller questions | desk sections, `index.html` |
| 13 | Product catalog research script, repeatable | `tools/build-products.mjs`, documented in `tools/README.md` |
| 14 | Copy pass (no stale "≈11%"/"$1 floor" text) | verified: no matches in `README.md`/`DESIGN.md` |

No open P0/P1 carryover as of this writing — the e2e harness (`test/e2e/harness.mjs`)
already targets the rewritten page's ids (`body[data-view]`, `.strip`, `#glance`); it is
not stale.

Open follow-ups from the 2026-08-10 expert panel re-review (buyer-side S&H in verdicts,
unset-vs-zero fulfillment, restore fidelity, a11y) are planned in
[docs/panel-review-2026-08-10.md](docs/panel-review-2026-08-10.md); none are implemented yet.

## Delegated decisions — how they resolved

The feedback doc explicitly left these to the implementer:

- **Mode-switch mechanism:** `body[data-view="buyer"|"seller"]` toggle in the header,
  driving `.buyer-only`/`.seller-only` visibility via CSS. Default view is buyer.
- **"Unreliable sealed price" heuristic:** absence of a sealed listing from tcgcsv is the
  trigger (tcgcsv exposes no sales-volume signal to threshold against) — see
  `chooseBasis` and its banner copy in `index.html`.
- **Orders model (surfaced mid-implementation, not in the original feedback doc):** an
  order is a buyer-chosen group of one or more won slots, not fixed one-order-per-slot —
  fulfillment cost and buyer S&H are editable per order. See
  `docs/adr/0001-orders-are-buyer-grouped-not-per-slot.md`.

Top-contributor count (10) was an owner ruling, not delegated — kept fixed.

## Out of scope (still binding)

- No change to price sources (Scryfall, tcgcsv.com) or the CORS relay approach.
- No build tooling — stays a single static `index.html`.

## Current phase beyond this spec

Collation format v2 (per-set sheet weights, foreign/bonus-sheet resolution, published-rate
validation) is active, ongoing work tracked in `tools/README.md` and `docs/adr/` — it is
infrastructure for pricing accuracy, not a product-facing requirement from the feedback
doc, so it is not duplicated here.
